#!/bin/sh

set -e

if [ "$1" = "configure" ]; then
  update-alternatives \
    --install /usr/games/zcode-interpreter zcode-interpreter \
     /usr/games/fizmo-remglk-launcher 50
fi

#DEBHELPER#

exit 0
